-to other lect
	-be able to explain parts of a URI/L

webb app / used by humans
	-clint decodes snd displays the app
	-has a layout via GUI from html

self describing web services has good documentation
	-in what form though?



Web service architectural styles

RPC - ?
	-protocols - so functions to be called and parameters

Simple Object Application Progr?
	-can have own name spaces such as vars and ?strings?

REpresentational State Transfer - REST(ful?)
	-WE ARE USING REST style
	- not rest api
	-a style, so exposes ?info?
	-can also be used for web ?
	-adressability?
		- not "getabookmark" in URI like in RPC, rather "bookmark"
	-
	-processing of res
		-
	-cahceability
		-forward proxy - proxy
			-between client and internet?
		-reverse proxy
			-between iternet and server
		-so local cache - proxy cache - reverse proxy cache
	-drawbacks
		-stateless
		-each individual res needs its own separate request

Graph Query Language
	-can query several res in 1 request

read graphql vs rest from link



DJANGO

views are functions w/ input output

models are databases

like MVC but view cant change database?







