npm create vite@latest lab1-app -- --template react

npm run dev
	- to run ur client side
	-in folder that has src!

if vsc uses powershell, it needs admin rights


Javascript

set of key and value pairs 
	-did he say objects are this

arrays can store mixed values, therefore also functions: like the ?object ?methods

var has larger scope than let and const?

skriv själv all kod om JS för ti öva å ha referens efter kurs
	-what happens if combining just without spread operator?
		-nested arrays


use VSC terminal, not cmd

figure out SimpleFunctionalComponents is not defined, so it works like SimpleClassComponents

make origin? the cloud up to date


for react shortcuts and e.g. boilerplate - npm rn dev?
	-extension: ES7 + React/Redux/react-Native snippets

props are nothing but parameters to a function


















